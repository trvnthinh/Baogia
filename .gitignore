# macOS / Windows junk
.DS_Store
Thumbs.db

# Editor configs
.vscode/
.idea/

# Backup / export files (data thật nằm trên Google Sheet, không cần commit JSON export)
*.json.bak
pmc_baogia_*.json

# Node (nếu sau này có thêm tooling)
node_modules/
